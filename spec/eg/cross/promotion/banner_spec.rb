# frozen_string_literal: true

RSpec.describe Eg::Cross::Promotion::Banner do
  it "has a version number" do
    expect(Eg::Cross::Promotion::Banner::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
